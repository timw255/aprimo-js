/**
 * A hyperlink with display text and URL.
 */
export interface Hyperlink {
  /** The text to display for the link. */
  displayText: string;
  /** The target URL. Format: uri. */
  url: string;
}
