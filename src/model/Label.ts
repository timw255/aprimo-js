/**
 * A localized label with language identifier.
 */
export interface Label {
  /** The ID of the language for this label. */
  languageId: string;
  /** The localized text value. */
  value: string;
}
