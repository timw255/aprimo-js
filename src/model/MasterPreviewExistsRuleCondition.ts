/**
 * Representation of record master preview exists rule condition.
 */
export interface MasterPreviewExistsRuleCondition {
  /**
   * Gets the data type of this rule condition.
   */
  conditionType: "MasterPreviewExists";
  /** Index of a rule condition in collection. Format: int32. */
  index?: number;
}
